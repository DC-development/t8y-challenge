/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var ChallengeRouter = Backbone.Router.extend({
        routes: {
        }
    });

    return ChallengeRouter;
});
